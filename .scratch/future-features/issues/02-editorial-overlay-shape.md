# Editorial overlay shape

Tags: wayfinder:grilling
Status: closed

## Resolution

- File format: one Markdown file per org with YAML frontmatter for structured overrides + body for editorial notes, git-versioned alongside scraped data.
- Merge precedence: field-level; allow-listed editorial fields override scraped data, factual fields stay scrape-wins unless explicitly pinned.
- Ownership/editing flow: non-technical editors via lightweight admin UI that writes the files/commits.

## Question

Grill the editorial overlay shape: file format, merge precedence, ownership.

## Blocked by

None (frontier — can start immediately)
